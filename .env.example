# Copia como .env y coloca tu token real
BOT_TOKEN=REEMPLAZAR
EXPIRY_HOURS=24
AUTH_FORWARDERS=11111111,22222222

# Límite de tamaño de archivo .txt (bytes)
MAX_BYTES=2000000

# Rate limit (segundos entre comandos por usuario)
MIN_INTERVAL=1.0

# Frecuencia de purga de claves usadas/expiradas (segundos)
AUTO_PURGE_INTERVAL=3600
